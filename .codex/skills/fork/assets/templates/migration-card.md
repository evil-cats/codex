# Миграция Codex fork на `X.Y.Z`

Статус: `draft`

## Контекст

- Upstream tag: `rust-vX.Y.Z`
- Branch: `hermione-X.Y.Z`
- Local checkout: `<repo-root>`

## Таблица покрытия

| Карточка | Статус | Ответственный результат | Проверки |
| --- | --- | --- | --- |
| `docs/fork/<card>.md` | `pending` | - | - |

## Результаты по карточкам

### `docs/fork/<card>.md`

- Статус:
- Что изменилось:
- Какие проверки нужны:
- Blockers:

## Общие gates

- `fork preflight`:
- `fork format --check`:
- `fork generators`:
- `fork tests --mode cards`:
- `fork build-fast`:
- `fork tests --mode full`:
