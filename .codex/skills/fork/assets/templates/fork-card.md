---
id: fork-<slug>
status: active
created: YYYY-MM-DD
updated: YYYY-MM-DD
---

# <Название fork-доработки>

## Обзор

<Коротко опиши доработку и её область действия.>

## Зачем это нужно

<Какая проблема решается и почему это должно жить в fork.>

## Карта файлов

| Файл | Роль |
| --- | --- |
| `<path>` | <Что в этом файле принадлежит fork-доработке> |

## Итоговый контракт

<Runtime/model-visible/API/config/schema поведение и инварианты, которые нельзя
сломать при переносе.>

## Архитектурное решение

<Опиши устройство реализации и причины только тех решений, которые нельзя
понять из итогового контракта. Существенную отклонённую альтернативу упомяни
рядом с выбранным решением, если без неё причина останется неясной.>

## Порядок повторения при переносе

1. <Что проверить в новом upstream checkout.>
2. <Какие файлы или контракты перенести.>
3. <Как убедиться, что перенос не восстановил лишний старый diff.>

## Проверки

<Если назначение запусков неочевидно из `purpose`, кратко поясни здесь
регрессионные сценарии. Не записывай результаты конкретных запусков, историю
падений, сборок или установок.>

Данные card-level запусков для `fork tests`:

```json
{
  "schema": "fork-tests.v1",
  "tests": [
    {
      "purpose": "<какое поведение проверяется>",
      "argv": ["just", "test", "-p", "<crate>", "<test-filter>"]
    }
  ]
}
```

<Если автоматизированного card-level теста нет, вместо блока укажи
`manual-required: <причина>` или `not-applicable: <причина>`. Дополнительный
skill-owned gate указывай только когда он действительно нужен этой доработке.>

## Риски и ограничения

- <Постоянный риск, ограничение или известный edge case реализации.>
