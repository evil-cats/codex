# <Название fork-доработки>

Статус: `active`

## Обзор

<Коротко опиши fork-доработку и её текущий статус.>

## Зачем это нужно

<Какая проблема решается и почему это должно жить в fork.>

## Карта файлов

| Файл | Роль |
| --- | --- |
| `<path>` | <Что в этом файле принадлежит fork-доработке> |

## Итоговый контракт

<Runtime/model-visible/API/config/schema поведение и инварианты, которые нельзя
сломать при переносе.>

## Архитектурное решение

<Почему выбран этот подход и какие альтернативы отклонены.>

## Порядок повторения при переносе

1. <Что проверить в новом upstream checkout.>
2. <Какие файлы или контракты перенести.>
3. <Как убедиться, что перенос не восстановил лишний старый diff.>

## Проверки

### Смысловое покрытие

| Контракт | Обязательность | Где покрывается |
| --- | --- | --- |
| <Что должно быть проверено> | `required` | <метка `fork tests`, test target или подтверждение> |

### Владелец исполняемой карты

| Владелец | Метка `fork tests` | Статус |
| --- | --- | --- |
| `fork tests` | `<card-id>` | `required` |

Данные card-level запусков для `fork tests`:

```json
{
  "schema": "fork-tests.v1",
  "tests": [
    {
      "purpose": "<короткая цель проверки>",
      "argv": ["just", "test", "-p", "<crate>", "<test-filter>"]
    }
  ]
}
```

### Дополнительные gates

| Проверка | Когда нужна | Статус |
| --- | --- | --- |
| `fork generators` | <Сгенерированные артефакты> | `required` / `not-applicable: <причина>` |
| `fork build-fast` | <Сборочный артефакт> | `required` / `not-applicable: <причина>` |

### Исторические результаты

| Проверка | Результат | Примечание |
| --- | --- | --- |
| <Фактически выполненная проверка> | `passed` / `failed` / `skipped` | <Причина, если нужно> |

Точные `just`/`cargo`/`fork ... --...` команды фиксируй только здесь как
исторический результат уже выполненного запуска. Нормативный запуск должен
оставаться у skill-owned владельца из разделов выше.

### Известные падения и пропуски

- <Если нет, напиши `Нет`.>

## Runtime, сборка и установка

<Если доработка проверялась установленным бинарем, release-fast artifact,
runtime-командой или ручной проверкой, зафиксируй здесь. Если не применимо,
напиши `Не применимо` с причиной.>

## Риски и ограничения

- <Риск, ограничение или известный edge case.>

## Проверка покрытия

| Смысловой пункт | Статус | Где покрыто |
| --- | --- | --- |
| <Пункт контракта или решения> | `перенесено в карточку` | <Раздел или ссылка> |

## Открытые вопросы

- <Если нет, напиши `Нет`.>
