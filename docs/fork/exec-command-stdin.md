---
id: fork-exec-command-stdin
status: active
created: 2026-08-06
updated: 2026-08-06
source_scope: discussion-2026-08-06
---

# Начальный `stdin` для `exec_command`

## Обзор

Эта карточка владеет доработкой fork Hermione, которая добавляет в основной инструмент
`exec_command` опциональный строковый аргумент `stdin`. Аргумент передаёт
процессу начальный текст напрямую через стандартный ввод и устраняет
необходимость создавать отдельную команду-производитель вроде `echo` или
`printf` только ради подачи данных в shell.

| Поле | Значение |
| --- | --- |
| Статус карточки | `active` |
| Состояние реализации | реализована и прошла локальные проверки карточки на `hermione-0.146.0`; сборка и проверка выполнения ветвей `cfg(windows)` не проводились |
| Инструмент | `exec_command` |
| Новый аргумент | `stdin?: string` |
| Формат данных | UTF-8-текст из строки JSON; не бинарный транспорт |
| Неявный перевод строки | не добавляется |
| Поведение при `tty=false` | передать весь текст и закрыть stdin, сформировав EOF |
| Поведение при `tty=true` | передать начальные символы в PTY и оставить stdin открытым для `write_stdin` |
| Проверка разрешения команды | анализирует только `cmd`; `stdin` не является сегментом команды |
| Ограничения sandbox | продолжают ограничивать действия процесса; содержимое `stdin` отдельно не анализируется |
| Удалённое выполнение | только при объявленной возможности `initial_stdin`; её отсутствие даёт явную ошибку до запуска |
| Источник истины | эта карточка и файлы-владельцы из раздела `Карта файлов` |

Главный контракт:

- `stdin` является данными вызова инструмента, а не фрагментом команды shell;
- значение никогда не соединяется с `cmd`, не экранируется для shell и не
  превращается в дополнительный сегмент конвейера;
- парсер команд, `prefix_rule`, кэш разрешений и классификация команды для
  sandbox получают тот же `cmd`, который получили бы без `stdin`;
- эффективный sandbox файловой системы и сети процесса остаётся неизменным и
  ограничивает последствия обработки входных данных;
- опущенный `stdin` полностью сохраняет прежнее поведение `exec_command`;
- предоставленный `stdin: ""` является явным пустым входным потоком: при
  `tty=false` процесс получает немедленный EOF;
- ошибка передачи начального ввода не приводит к повторному запуску команды и
  не должна молча превращаться в запуск без данных.

## Зачем это нужно

До этой доработки модель могла подать данные программе через конвейер команд shell:

```text
echo "hello" | wc -c
```

Однако парсер разрешений рассматривает конвейер как цепочку исполняемых команд.
В примере отдельно проверяются `echo` и `wc`, хотя пользовательское намерение
состоит в запуске одного потребителя `wc`, а `hello` является только входными
данными. При динамическом содержимом это создаёт повторяющиеся запросы разрешения
для синтетической команды-производителя и делает сохранённый `prefix_rule`
бесполезным для пакетной обработки разных входов.

`write_stdin` не является полноценной заменой:

- сначала требуется запустить долгоживущую сессию и получить `session_id`;
- без нового аргумента обычный non-TTY `exec_command` запускается с закрытым stdin;
- интерактивный PTY меняет терминальную семантику, включая построчную обработку,
  отображение введённых символов и управляющие символы;
- для потребителей, читающих поток до EOF, нужен явный конец входа, а не
  остающаяся открытой интерактивная сессия.

Новый аргумент позволяет выразить то же намерение без команды-производителя для shell:

```json
{
  "cmd": "wc -c",
  "stdin": "hello"
}
```

Ожидаемый stdout равен `5`: `exec_command` передаёт ровно пять UTF-8-байтов и
закрывает non-TTY stdin. Для поведения, эквивалентного `echo "hello"`, модель
явно передаёт `"hello\n"`, и ожидаемый stdout равен `6`.

## Карта файлов

### Файлы-владельцы реализации

| Файл | Ответственность |
| --- | --- |
| `codex-rs/core/src/tools/handlers/shell_spec.rs` | Добавляет видимое модели опциональное поле `stdin` в схему `exec_command` и описывает семантику UTF-8, EOF и TTY |
| `codex-rs/core/src/tools/handlers/shell_spec_tests.rs` | Проверяет схему, опциональность поля и точное описание нового аргумента |
| `codex-rs/core/src/tools/handlers/unified_exec.rs` | Добавляет `stdin: Option<String>` в `ExecCommandArgs` без изменения разбора `cmd` |
| `codex-rs/core/src/tools/handlers/unified_exec/exec_command.rs` | Сохраняет `stdin` после перезаписи команды обработчиком hook, передаёт его в unified exec и не включает в данные проверки разрешения команды |
| `codex-rs/core/src/tools/handlers/unified_exec_tests.rs` | Проверяет разбор аргументов, перезапись команды обработчиком hook и отсутствие потери или подмены `stdin` |
| `codex-rs/core/src/tools/handlers/apply_patch.rs` | Явно отклоняет перехваченный `apply_patch`, если вызов `exec_command` одновременно передал `stdin`, до изменения файлов |
| `codex-rs/core/src/tools/handlers/shell.rs` | Сохраняет прежнее разрешённое поведение перехвата `apply_patch` для устаревшего обработчика `shell` |
| `codex-rs/core/src/unified_exec/mod.rs` | Добавляет начальный ввод в `ExecCommandRequest` |
| `codex-rs/core/src/tools/runtimes/unified_exec.rs` | Проводит начальный ввод через исполнитель unified exec, оставляя `UnifiedExecApprovalKey` основанным только на `command`, `cwd`, `permissions` и TTY |
| `codex-rs/core/src/unified_exec/process_manager.rs` | Открывает stdin, передаёт начальный ввод, закрывает non-TTY-поток и сохраняет PTY открытым |
| `codex-rs/core/src/unified_exec/process_manager_tests.rs` | Проверяет передачу начального ввода в удалённый `ExecParams` |
| `codex-rs/core/src/unified_exec/async_watcher_tests.rs` | Совместимо инициализирует расширенный `ProcessDriver` в существующей тестовой инфраструктуре |
| `codex-rs/core/src/unified_exec/mod_tests.rs` | Обновляет существующие тестовые вызовы диспетчера процессов для нового опционального аргумента |
| `codex-rs/core/tests/suite/mod.rs` | Регистрирует отдельный интеграционный модуль доработки |
| `codex-rs/core/tests/suite/unified_exec_initial_stdin.rs` | Проверяет полный путь вызова инструмента, EOF, точность текста, TTY, объявленную возможность удалённого сервера и отказ `apply_patch` до мутации |
| `codex-rs/utils/pty/src/process.rs` | Предоставляет подтверждаемую запись начального ввода и возвращает фактическую ошибку поддерживающей реализации |
| `codex-rs/utils/pty/src/lib.rs` | Экспортирует подтверждаемый запрос начального stdin для транспортов с внешним драйвером |
| `codex-rs/utils/pty/src/pipe.rs` | Подтверждает результат `write_all` и `flush` для начального ввода процесса с pipe |
| `codex-rs/utils/pty/src/pty.rs` | Подтверждает результат начальной записи для обычного PTY и Unix PTY с унаследованными дескрипторами |
| `codex-rs/utils/pty/src/tests.rs` | Детерминированно проверяет передачу низкоуровневой ошибки записи вызывающему коду |
| `codex-rs/exec-server-protocol/src/protocol.rs` | Добавляет возможность `initial_stdin` и опциональное поле начального ввода в `ExecParams` для удалённого окружения |
| `codex-rs/exec-server/src/local_process.rs` | Передаёт начальный ввод в созданный процесс и закрывает канал записи non-TTY после отправки |
| `codex-rs/exec-server/tests/exec_process.rs` | Проверяет локальный и удалённый транспорт exec-server, EOF и соблюдение ограничений sandbox |
| `codex-rs/windows-sandbox-rs/src/unified_exec/backends/legacy.rs` | Подтверждает результат прямого Windows `WriteFile` и возвращает ошибку начальной записи |
| `codex-rs/windows-sandbox-rs/src/unified_exec/backends/elevated.rs` | Подключает канал подтверждаемого начального stdin к транспорту привилегированного `runner` |
| `codex-rs/windows-sandbox-rs/src/unified_exec/backends/windows_common.rs` | Объединяет обычные и подтверждаемые записи, подтверждает IPC-кадр и принимает результат `runner` |
| `codex-rs/windows-sandbox-rs/src/elevated/ipc_framed.rs` | Добавляет `InitialStdin` и `InitialStdinResult` во внутренний протокол Windows `runner` |
| `codex-rs/windows-sandbox-rs/src/bin/command_runner/win.rs` | Выполняет полную запись в stdin дочернего процесса и отправляет фактический результат родительскому процессу |
| `codex-rs/windows-sandbox-rs/src/lib.rs` | Экспортирует `InitialStdinResultPayload` для исполняемого файла `codex-command-runner` |
| `codex-rs/windows-sandbox-rs/src/unified_exec/mod.rs` | Открывает только для тестов доступ к объединителю каналов stdin |
| `codex-rs/windows-sandbox-rs/src/unified_exec/tests.rs` | Актуализирует тестовые литералы `ProcessDriver` и проверяет сохранение обычного протокола stdin/EOF для `runner` |
| `docs/fork/exec-command-stdin.md` | Документ-владелец для переноса, проверки и отката доработки |

Новое поле `ExecParams.initial_stdin` также потребовало совместимых
значений `None` в существующих литералах Rust:

- `codex-rs/exec-server/src/client.rs`;
- `codex-rs/exec-server/src/client/tests/network_policy_tests.rs`;
- `codex-rs/exec-server/src/environment.rs`;
- `codex-rs/exec-server/src/process_sandbox_tests.rs`;
- `codex-rs/exec-server/src/server/handler/tests.rs`;
- `codex-rs/exec-server/src/server/processor.rs`;
- `codex-rs/exec-server/tests/relay.rs`;
- `codex-rs/rmcp-client/src/stdio_server_launcher.rs`.

Расширение `ProcessDriver.initial_stdin_tx` также потребовало явных `None` в
существующих тестовых литералах `ProcessDriver`:

- `codex-rs/core/src/unified_exec/async_watcher_tests.rs`;
- `codex-rs/exec-server/src/local_process.rs`;
- `codex-rs/utils/pty/src/tests.rs`;
- `codex-rs/windows-sandbox-rs/src/unified_exec/tests.rs`.

Карта сверена с фактическими изменениями реализации. При переносе на новую
версию upstream её нужно сверять заново, если транспорт процесса или литералы
протокола перемещены.

### Намеренно неизменяемые зоны

- `write_stdin` сохраняет контракт последующего взаимодействия с уже запущенной
  сессией и не становится обязательным промежуточным шагом для начального ввода;
- устаревший `shell_command` не получает новый аргумент;
- протокол `apply_patch` не расширяется начальным вводом через эту карточку;
- сбор вывода из stdout/stderr, усечение, сохранение вывода во внешний файл и
  `max_output_tokens` не меняются;
- профиль разрешений файловой системы и сети, а также правила
  привилегированного запуска не ослабляются;
- необработанный бинарный ввод, кодирование base64, потоковая передача больших
  файлов и передача файлового дескриптора не входят в область карточки.

## Итоговый контракт

### Спецификация инструмента

`exec_command` получает новое необязательное свойство:

```text
stdin?: string
```

Видимое модели описание должно сообщать:

1. Значение передаётся процессу как UTF-8 без неявного `\n`.
2. При `tty=false` после текста следует EOF.
3. При `tty=true` значение является начальными PTY-символами, а сессия
   остаётся доступной через `write_stdin`.
4. Параметр предназначен для данных, которые иначе пришлось бы подавать через
   отдельную команду-производитель или конвейер shell.

`stdin` не является обязательным полем. Отсутствие аргумента должно сохранять
прежнее поведение на уровне байтов и жизненного цикла.

### Кодирование и точность данных

- JSON-строка декодируется в Rust `String`, затем передаётся как UTF-8-байты;
- пустая строка допустима;
- `\n`, `\r`, NUL и другие допустимые символы JSON-строки передаются как байты
  после стандартного декодирования JSON;
- инструмент не добавляет разделитель, перевод строки или управляющий символ
  терминала;
- бинарные данные, для которых требуется сохранить произвольные октеты вне
  UTF-8, должны получить отдельный будущий контракт.

### Жизненный цикл без TTY

Если `tty=false` и `stdin` предоставлен:

1. Sandbox и проверка разрешения обрабатывают `cmd` обычным способом.
2. Процесс запускается с pipe для stdin.
3. Весь `stdin` передаётся в канал записи в исходном порядке.
4. Канал записи закрывается сразу после успешной передачи, поэтому
   процесс-потребитель видит EOF.
5. `exec_command` ожидает вывод или завершение до обычного `yield_time_ms`; если
   процесс продолжает работать после EOF, применяется прежний жизненный цикл
   сессии.

`stdin: ""` пропускает запись байтов, но всё равно открывает и закрывает pipe,
чтобы процесс получил пустой поток и EOF. Опущенный `stdin` не требует pipe и
сохраняет текущий путь запуска с закрытым stdin.

### Жизненный цикл с TTY

Если `tty=true` и `stdin` предоставлен:

- текст передаётся после успешного запуска PTY;
- stdin не закрывается автоматически;
- построчная обработка терминала может преобразовывать или буферизовать ввод;
- последующий `write_stdin` продолжает работать по обычному `session_id`;
- процесс-потребитель, которому нужен точный конечный поток и EOF, должен
  использовать `tty=false`.

### Ошибки и атомарность запуска

Если процесс не удалось запустить, `stdin` никуда не передаётся. Если процесс
запустился, но начальный ввод не удалось полностью принять:

- инструмент возвращает явную ошибку начального stdin;
- процесс завершается и удаляется из реестра процессов;
- команда не перезапускается автоматически, потому что она уже могла выполнить
  побочный эффект до ошибки записи;
- частичная запись не выдаётся за успешную передачу полного значения.

Для удалённого окружения клиент обязан проверить возможность `initial_stdin` до
запуска. Если она отсутствует, `exec_command` возвращает понятную ошибку
совместимости и не отправляет запрос со значением, которое старый exec-server
мог бы молча проигнорировать.

### Граница sandbox и проверки разрешений

`stdin` не должен участвовать в:

- разборе shell и разделении `cmd` по `|`, `&&`, `||`, `;` или границам
  подоболочки;
- `canonicalize_command_for_approval`;
- `UnifiedExecApprovalKey`;
- сопоставлении с `prefix_rule`;
- классификации безопасности команды;
- построении `hook_command` для обработчиков hook Bash;
- тексте команды в диалоге подтверждения.

Следовательно, вызов с `cmd: "wc -c"` и любым новым `stdin` использует то же
разрешение команды, что и `wc -c` без начального ввода. Содержимое не создаёт
синтетический сегмент `echo`/`printf` и не требует отдельного подтверждения.

Это не означает выполнение вне sandbox. Запущенный процесс получает те же
ограничения файловой системы и сети, а также те же дополнительные разрешения,
что и при вызове без `stdin`. Если программа интерпретирует вход как команды,
последствия этих команд всё равно ограничиваются sandbox процесса.

### Обработчики hook, события и сохранение данных

- Bash `PreToolUse` продолжает получать только `{"command": cmd}`;
- перезапись обработчиком hook может изменить `cmd`, но обязана сохранить
  исходный `stdin` без изменения;
- начальный ввод не создаёт отдельный `TerminalInteractionEvent`, потому что
  исходный вызов инструмента уже содержит значение;
- события начала и завершения команды продолжают описывать её жизненный цикл и
  не дублируют полный ввод;
- исходные аргументы инструмента, включая `stdin`, могут сохраняться в JSONL
  журнала выполнения и трассировках; параметр не является каналом для секретов.

Если `exec_command` перехвачен специальной веткой `apply_patch`, `stdin` нельзя
молча игнорировать. Текущая реализация должна вернуть понятную ошибку для этой
комбинации; расширение отдельного транспорта `apply_patch` принадлежит другому
решению.

## Архитектурное решение

### Передавать ввод как данные процесса

`stdin` проходит отдельным полем через аргументы инструмента, `ExecCommandRequest`,
`UnifiedExecRequest` и транспорт запуска процесса. Оно не преобразуется в
исходный текст shell и не встраивается в переменную окружения или argv.

Для прямого локального запуска диспетчер процессов открывает канал записи при
`stdin.is_some()`. Для удалённого запуска опциональный начальный ввод входит в
`ExecParams` exec-server и обрабатывается исполнителем сразу после запуска.
Такой путь сохраняет семантику конечного потока и позволяет серверу закрыть
канал записи без дополнительной гонки между `exec`, `process/write` и новым RPC.

### Проверка возможности удалённого окружения

`EnvironmentCapabilities` получает логическое поле `initial_stdin`. Новый
exec-server объявляет `true`; у старого сервера отсутствующее поле получает
значение `false`. Core проверяет эту возможность до удалённого запуска, если
`stdin` предоставлен.

Эта проверка требуется не для прямого локального запуска, а для защиты от тихой
потери данных при несовпадении версий клиента и exec-server.

### Идентичность разрешения остаётся основанной только на команде

Проверка разрешения отвечает на вопрос, какой процесс и с какими разрешениями
можно запустить. `stdin` является последующим вводом этого процесса и намеренно не
расширяет идентичность команды. Это позволяет повторно использовать разрешение
для одного потребителя с разными входными данными.

При этом общий интерпретатор вроде `bash`, `sh`, `python -` или PowerShell может
трактовать stdin как исполняемый исходный текст. Сохранённое разрешение для
такого общего интерпретатора остаётся опасным и не становится безопасным
благодаря этой доработке. Системный sandbox остаётся последней границей
последствий.

## Отклонённые альтернативы

| Альтернатива | Почему отклонена |
| --- | --- |
| `echo "$value" \| consumer` | Добавляет команду-производитель, меняет семантику перевода строки и вызывает отдельную проверку разрешения |
| `printf %s "$value" \| consumer` | Сохраняет данные точнее `echo`, но всё равно является отдельным сегментом команды shell |
| Here-document или here-string | Встраивает данные в исходный текст shell и не устраняет неоднозначность разбора и разрешения команды |
| Переменная окружения | Имеет ограничения платформы и размера, видна дочерним процессам и не воспроизводит семантику stdin |
| Временный файл и `< file` | Создаёт файловый артефакт, требует очистки и проверки перенаправления и пути |
| Только `tty=true` плюс `write_stdin` | Требует двух вызовов инструмента, семантики PTY и ручного EOF; не подходит обычному non-TTY-потребителю |
| Добавить stdin только в `process/write` | Не даёт атомарного начального ввода и требует отдельного RPC закрытия stdin для конечного потока |
| Включить `stdin` в ключ разрешения | Вернёт повторное подтверждение для каждого динамического значения и не решит исходную проблему |
| Печатать полный `stdin` в диалоге подтверждения | Может раскрыть большие или чувствительные данные и смешивает разрешение процесса с содержимым ввода |
| Base64 или бинарный ввод в первой версии | Расширяет API за пределы запрошенного произвольного текста и требует отдельного контракта размера и кодирования |

## Порядок повторения при переносе

Для нового checkout следующей версии upstream:

1. Найти текущие структуры-владельцы спецификации инструмента и десериализации для
   `exec_command`.
2. Проверить актуальный путь `ExecCommandArgs` до исполнителя unified exec и
   запуска локального или удалённого процесса.
3. Добавить опциональный `stdin` в схему инструмента и структуры запроса, не
   меняя обязательные поля и поведение по умолчанию.
4. Сохранить основанную только на команде идентичность разрешения, данные hook
   и сопоставление с `prefix_rule`.
5. Реализовать локальную начальную запись: открыть stdin только при
   необходимости, передать все байты и закрыть канал записи non-TTY.
6. Расширить протокол exec-server опциональным начальным вводом и проверкой
   возможности; старый удалённый сервер должен давать явную ошибку совместимости
   до запуска.
7. Реализовать начальную запись и закрытие на стороне сервера с одинаковым
   поведением на Linux, macOS и Windows.
8. Не допустить тихого игнорирования в специальной ветке перехвата `apply_patch`.
9. Добавить проверки спецификации, обработчика, транспорта процесса, возможности
   удалённого сервера, EOF, разрешений и sandbox.
10. Сверить фактические изменения с разделом «Карта файлов», обновить результаты проверок и
    известные ограничения этой карточки.

## Проверки

### Смысловое покрытие

| Контракт | Обязательность | Где покрывается |
| --- | --- | --- |
| Схема инструмента содержит опциональное строковое поле `stdin` и не делает его обязательным | `required` | проверки `shell_spec` |
| Опущенный `stdin` сохраняет прежний путь запуска и закрытый non-TTY stdin | `required` | регрессионные проверки core unified exec |
| `stdin: "hello"` передаёт ровно пять UTF-8-байтов без перевода строки | `required` | интеграционная проверка core |
| `stdin: "hello\n"` сохраняет явно заданный перевод строки | `required` | интеграционная проверка core |
| `stdin: ""` создаёт пустой non-TTY-поток и EOF | `required` | проверки диспетчера процессов и exec-server |
| Non-TTY-потребитель, читающий до EOF, завершается без PTY и `write_stdin` | `required` | интеграционная проверка core |
| TTY получает начальные символы и остаётся доступным для записи через `write_stdin` | `required` | проверка core unified exec |
| `stdin` не добавляется в сегменты команды, ключ разрешения или сопоставление `prefix_rule` | `required` | регрессионная проверка approval/policy |
| Перезапись обработчиком hook меняет только `cmd` и сохраняет `stdin` на уровне байтов | `required` | проверки обработчика unified exec |
| Sandbox продолжает запрещать файловое или сетевое действие, инициированное входом | `required` | проверка sandbox в exec-server |
| Ошибка начальной записи завершает процесс и не повторяет команду | `required` | проверка транспорта процесса |
| Прямая и привилегированная реализации Windows возвращают результат фактической записи, а не постановки в очередь | `required` | платформенные проверки IPC и модулей; на текущем хосте Linux не запускались |
| Удалённый сервер с возможностью `initial_stdin` передаёт ввод и EOF | `required` | проверка паритета локального и удалённого exec-server |
| Удалённый сервер без возможности `initial_stdin` отклоняется до запуска с явной ошибкой | `required` | проверка удалённого окружения core |
| Перехват `apply_patch` не игнорирует предоставленный `stdin` | `required` | проверка обработчика или интеграционная проверка unified exec |
| Бинарные данные не поддерживаются и не заявляются в описании | `required` | проверка спецификации |

### Владелец исполняемой карты

| Владелец | Метка | Статус |
| --- | --- | --- |
| `fork tests` | `fork-exec-command-stdin` | `required` |

Исполняемая карта проверок карточки хранится в блоке `fork-tests.v1`, который
читает `fork tests`. Карточка не является инструкцией запуска проверок.

```json
{
  "schema": "fork-tests.v1",
  "tests": [
    {
      "purpose": "схема инструмента, обработчик, разрешения и сквозной контракт stdin",
      "argv": ["just", "test", "-p", "codex-core", "exec_command_stdin"]
    },
    {
      "purpose": "локальный и удалённый транспорт начального stdin в exec-server",
      "argv": ["just", "test", "-p", "codex-exec-server", "initial_stdin"]
    },
    {
      "purpose": "возможность протокола exec-server и сериализация",
      "argv": [
        "just",
        "test",
        "-p",
        "codex-exec-server-protocol",
        "initial_stdin"
      ]
    },
    {
      "purpose": "подтверждение начальной записи stdin в реализации PTY",
      "argv": ["just", "test", "-p", "codex-utils-pty", "initial_stdin"]
    }
  ]
}
```

### Дополнительные gates

| Проверка | Когда нужна | Статус |
| --- | --- | --- |
| `fork generators` | Новых артефактов схем config/app-server не планируется | `not-applicable` |
| `fork format` | После реализации на Rust | `passed` |
| `fork fix` | После реализации на Rust для изменённых пакетов | `passed` |
| `fork tests` | Полный регрессионный режим перед финализацией изменений общих `core`/protocol при отдельном согласии пользователя | `not-run` |
| `fork build-fast` | После целевых проверок перед установкой или передачей сборки | `passed` |
| Сборка и запуск ветвей `cfg(windows)` | При доступном хосте Windows или кросс-компиляции | `not-run` |

### Исторические результаты

| Проверка | Результат | Примечание |
| --- | --- | --- |
| Проектирование карточки на `hermione-0.146.0` | `passed` | Сверены текущие спецификация инструмента, обработчик, диспетчер процессов unified exec, pipe stdin в exec-server и возможность удалённого сервера; реализация и проверки выполнения ещё не проводились |
| Первый прогон проверок карточки после рефакторинга подтверждаемой записи | `failed`, затем исправлен | Тестовый код `codex-utils-pty` не выводил тип канала после сокрытия полей `InitialStdinWrite`; добавлен явный тип, исполняемый код не запускался |
| `fork tests --mode cards --card fork-exec-command-stdin --version 0.146.0` | `passed` | Прошли 12 сценариев core, 8 exec-server, 2 protocol и 1 `codex-utils-pty`; проверены локальное и удалённое выполнение, EOF, TTY, sandbox, проверка возможности и передача ошибки низкоуровневой записи |
| `fork fix` для `codex-core`, `codex-exec-server`, `codex-exec-server-protocol`, `codex-rmcp-client`, `codex-utils-pty`, `codex-windows-sandbox` | `passed` | Несвязанные автоисправления вне карты файлов исключены из итогового diff |
| `fork format --check` и `git diff --check` | `passed` | Форматирование корректно, ошибок пробельных символов нет |
| `fork build-fast` | `passed` | Собран `codex-rs/target/release-fast/codex` версии `0.146.0` |

### Известные падения и пропуски

- Известных падений проверок карточки нет.
- Полный регрессионный `fork tests` не запускался: для него требуется отдельное
  согласие пользователя.
- Ветви `legacy`/`elevated` для Windows прошли разбор rustfmt и ручную
  сверку цепочки подтверждения, но не компилировались и не запускались:
  локальный набор инструментов не содержит целевой стандартной библиотеки
  Windows и компоновщика.
- Собранный исполняемый файл не устанавливался, поэтому проверочный вызов
  установленной версии с новым `stdin` не выполнялся.

## Выполнение, сборка и установка

Реализация и проверки выполнены в ветке `hermione-0.146.0` для версии
`0.146.0`. `fork build-fast` собрал
`codex-rs/target/release-fast/codex`; проверки протокола, локального и удалённого
выполнения прошли. Установка исполняемого файла и проверочный вызов установленной
версии остаются отдельными действиями и не выполнялись без прямой просьбы
пользователя.

## Риски и ограничения

- `stdin` сохраняется в исходных аргументах инструмента и журнале rollout; секреты
  передавать через него нельзя без отдельного контракта редактирования.
- Большой текст увеличивает объём данных вызова инструмента и память процесса;
  отдельный контракт потокового или файлового ввода в первой версии отсутствует.
- Интерпретатор может выполнить stdin как исходный текст программы. Проверка
  разрешения только по команде не анализирует этот текст; сохранённое разрешение общих
  интерпретаторов требует особой осторожности.
- PTY не гарантирует точного на уровне байтов терминального поведения; точный
  конечный ввод требует `tty=false`.
- Несовпадение версий удалённого exec-server должно обнаруживаться проверкой
  возможности `initial_stdin`, иначе существует риск тихой потери ввода.
- Ошибка после частичной записи не может отменить уже выполненные процессом
  действия; поэтому автоматический повтор запрещён.
- Ввод не отображается в диалоге разрешения команды. Пользователь подтверждает
  исполняемый файл и разрешения, а не каждое значение ввода.

## Проверка покрытия

| Согласованный пункт | Статус |
| --- | --- |
| Добавить `stdin` в `exec_command` | реализовано и проверено |
| Принимать произвольный текст | реализовано как UTF-8 JSON-строка; бинарный ввод намеренно не добавлен по решению пользователя |
| Передавать значение напрямую в stdin процесса | реализовано для локального и удалённого выполнения; транспорт Windows нельзя было собрать и запустить на текущем хосте |
| Не проверять значение как команду для sandbox | реализовано: разбор команды, ключ разрешения и `prefix_rule` остаются основаны только на `cmd` |
| Не ослаблять ограничения файловой системы и сети во время выполнения | проверено сценарием файлового sandbox, инициированным через входные данные |
| Исключить разрешения для команд-производителей `echo`/`printf` | реализовано: команда-производитель не создаётся |
| Поддержать EOF для эквивалента конвейера shell | реализовано и проверено для непустого и пустого non-TTY ввода |
| Сохранить интерактивный жизненный цикл PTY | реализовано и проверено последующим `write_stdin` |
| Не потерять ввод на старом удалённом exec-server | реализовано через проверку возможности `initial_stdin` и ошибку до `process/start` |
| Добавить исполняемую карту проверок | реализовано в `fork-tests.v1` |
