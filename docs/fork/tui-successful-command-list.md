---
id: fork-tui-successful-command-list
status: active
created: 2026-08-24
updated: 2026-09-02
---

# Список успешных команд в компактной TUI-группе

## Обзор

Эта карточка владеет fork-доработкой Hermione, которая сохраняет компактную
группировку последовательных успешных `exec`-команд под заголовком
`Ran N commands`, но показывает внутри группы сами команды с отображаемыми
аргументами. Обычная история TUI не показывает их `stdout` и `stderr`; полный
текст команд и вывод остаются доступны через `transcript` по `ctrl+t`.

## Зачем это нужно

Upstream Codex объединяет последовательные успешные команды в одну строку:

```text
• Ran 3 commands · ctrl + t to view transcript
```

Такое представление сокращает шум от вывода команд, но скрывает сами действия
агента. Пользователь видит только количество вызовов и вынужден открывать полный
`transcript` даже для ответа на простой вопрос: какие команды были выполнены.

Hermione сохраняет новую компактную группу, но возвращает наблюдаемость
команд:

```text
• Ran 3 commands · ctrl + t to view transcript
  ├ rg -n "pattern" docs/fork
  ├ git diff --check
  └ fork cards validate
```

## Карта файлов

| Файл | Роль |
| --- | --- |
| `codex-rs/tui/src/exec_cell/render.rs` | Рисует заголовок компактной группы, список команд без вывода, перенос длинных команд и неизменённые отдельные строки для остальных вызовов |
| `codex-rs/tui/src/exec_cell/model.rs` | Сохраняет текущие правила объединения успешных команд и границу `MAX_GROUPED_COMMANDS` |
| `codex-rs/tui/src/chatwidget/tests/exec_flow.rs` | Проверяет видимый список команд, отсутствие вывода в компактной истории, полный `transcript` и смешанный жизненный цикл |
| `codex-rs/tui/src/history_cell/tests.rs` | Проверяет список непосредственно на уровне `ExecCell` |
| `codex-rs/tui/src/chatwidget/snapshots/codex_tui__chatwidget__tests__exploring_step4_finish_cat_foo.snap` | Фиксирует компактный список из двух команд |
| `codex-rs/tui/src/chatwidget/snapshots/codex_tui__chatwidget__tests__exploring_step5_finish_sed_range.snap` | Фиксирует расширение компактного списка третьей командой |
| `codex-rs/tui/src/chatwidget/snapshots/codex_tui__chatwidget__tests__exploring_step6_finish_cat_bar.snap` | Фиксирует итоговый список из четырёх команд |
| `codex-rs/tui/src/history_cell/snapshots/codex_tui__history_cell__tests__coalesces_reads_across_multiple_calls.snap` | Фиксирует список непосредственно на уровне `ExecCell` |
| `docs/fork/tui-successful-command-list.md` | Хранит итоговый контракт, порядок переноса и регрессионные проверки уровня карточки |

Связанная карточка `docs/fork/tui-core-tool-activity.md` продолжает владеть
видимостью `read_file`, `get_thread_info`, `get_system_time` и смешанными
блоками `Exploring`/`Explored`. Эта карточка не переносит shell-команды в
`CoreToolActivity` и не меняет подписи core tools.

`codex-rs/tui/src/history_cell/patches.rs` не входит в область этой карточки:
`PatchHistoryCell` рендерит предпросмотр и transcript изменений файлов, а не
команды `ExecCell` или их компактную группу.

## Итоговый контракт

- Последовательные успешные вызовы с источником `ExecCommandSource::Agent` или
  `ExecCommandSource::UnifiedExecStartup` продолжают объединяться в одну
  компактную ячейку истории.
- Заголовок сохраняет форму
  `Ran N command(s) · ctrl + t to view transcript` и фактическое количество
  успешных команд в начале группы.
- Сразу под заголовком TUI показывает каждую учтённую успешную команду в
  исходном порядке. Для нескольких команд используются ветви `├` и `└`; для
  единственной показанной команды используется `└`.
- Строка команды включает её отображаемые аргументы. Внутренняя оболочка
  `bash -lc` скрывается тем же способом, что и в существующем рендерере
  одиночной команды; подсветка shell-синтаксиса сохраняется.
- Длинная или многострочная команда переносится и ограничивается существующим
  TUI-контрактом продолжений команды. Если команда целиком не помещается,
  компактная история показывает явный маркер пропущенных строк, а полный текст
  остаётся в `transcript`.
- В компактном списке успешных команд не показываются `stdout`, `stderr`,
  `(no output)`, `exit_code` и `duration` отдельных команд.
- `transcript_lines()` не меняется: `ctrl+t` по-прежнему показывает полный
  текст каждой команды, доступный сохранённый вывод, `exit_code` и `duration`.
- Выполняющиеся, ошибочные, отклонённые и интерактивные вызовы вне успешного
  префикса продолжают отображаться отдельными строками через существующий
  рендерер. Их текущие подписи и предпросмотр вывода эта доработка не меняет.
- Группы `Exploring`/`Explored`, core tool activity, пользовательские
  shell-команды и hook activity не меняются.
- Граница `MAX_GROUPED_COMMANDS` остаётся равной текущему upstream-значению.
  Доработка увеличивает высоту уже ограниченной группы, но не делает историю
  безграничной.

## Архитектурное решение

`ExecCell::compact_group_display_lines()` продолжает определять успешный префикс
группы по существующим условиям `source`, завершения и `exit_code == 0`. После
заголовка рендерер проходит по этому же срезу `ExecCall` и через
`command_text_display_lines()` строит для каждого вызова только представление
команды. Оставшаяся часть `self.calls` по-прежнему передаётся обычному рендереру
вызовов.

`command_text_display_lines()` владеет отображаемым текстом команды, подсветкой
shell-синтаксиса, переносом и ограничением продолжений. Её используют и рендерер
одиночной команды, и компактный список. Блок вывода остаётся только в
`command_call_display_lines()`; неоднозначного позиционного флага вроде
`include_output: bool` в интерфейсе нет.

Компактный рендерер добавляет структурные префиксы группы после переноса
команды. Для промежуточного элемента первая строка получает `"  ├ "`, а
продолжения — `"  │ "`; для последнего элемента используются `"  └ "` и
пробельный префикс продолжения. Это сохраняет визуальную принадлежность каждой
перенесённой строки своей команде и не смешивает её с выводом.

Модель `ExecCell`, правила накопления вызовов, лимит группы и полный `transcript`
не требуют изменения. Доработка относится только к компактному представлению.

## Порядок повторения при переносе

1. Найти актуальный рендерер сгруппированных успешных `exec`-команд и проверить,
   показывает ли upstream только `Ran N commands` либо уже перечисляет команды.
2. Если upstream предоставляет эквивалентный список без вывода и сохраняет
   полный `transcript`, удалить fork-отрисовку, оставив регрессионное покрытие или
   признав карточку неприменимой.
3. Иначе сохранить upstream-условия выбора успешного префикса и правила
   накопления `ExecCall`; не восстанавливать отдельные ячейки прежнего
   интерфейса истории.
4. Выделить общее форматирование текста команды из рендерера одиночного вызова
   и переиспользовать его для вложенных строк компактной группы.
5. Не переносить рендеринг `CommandOutput` в компактный список. Проверить, что
   обычное представление не содержит вывода, а `transcript` содержит и команды, и
   доступный вывод.
6. Сохранить отдельное отображение выполняющихся, ошибочных, отклонённых и
   интерактивных вызовов, а также существующие группы `Exploring`/`Explored` и
   core tools.
7. Обновить снимки `insta` для видимого списка, переноса длинных команд и
   смешанного успешного/неуспешного жизненного цикла.

## Проверки

Данные card-level запусков для `fork tests`:

```json
{
  "schema": "fork-tests.v1",
  "tests": [
    {
      "purpose": "компактная группа показывает успешные команды с аргументами без stdout и stderr",
      "argv": [
        "just",
        "test",
        "-p",
        "codex-tui",
        "compact_command_activity_lists_successes_without_output"
      ]
    },
    {
      "purpose": "полный transcript сохраняет команды и вывод компактной группы",
      "argv": [
        "just",
        "test",
        "-p",
        "codex-tui",
        "compact_command_activity_preserves_full_transcript"
      ]
    },
    {
      "purpose": "вызовы запуска unified exec сохраняют тот же компактный список успешных команд",
      "argv": [
        "just",
        "test",
        "-p",
        "codex-tui",
        "compact_command_activity_groups_unified_exec_startup_commands"
      ]
    },
    {
      "purpose": "длинные команды переносятся и ограничиваются внутри компактного списка",
      "argv": [
        "just",
        "test",
        "-p",
        "codex-tui",
        "compact_command_activity_wraps_listed_commands"
      ]
    },
    {
      "purpose": "ошибочные и выполняющиеся вызовы остаются видимыми рядом с успешным префиксом",
      "argv": [
        "just",
        "test",
        "-p",
        "codex-tui",
        "compact_command_activity_preserves_non_success_calls"
      ]
    },
    {
      "purpose": "непосредственный рендер ExecCell перечисляет все команды объединённой группы",
      "argv": [
        "just",
        "test",
        "-p",
        "codex-tui",
        "coalesces_reads_across_multiple_calls"
      ]
    }
  ]
}
```

## Риски и ограничения

- Группа до `MAX_GROUPED_COMMANDS` теперь занимает до одной видимой строки на
  короткую команду вместо одной строки на всю группу. Это осознанный компромисс
  ради наблюдаемости; полный вывод по-прежнему не заполняет обычную историю.
- Ограничение продолжений означает, что очень длинные или многострочные команды
  могут отображаться не полностью. Маркер сокращения и `transcript`
  должны явно сохранять путь к полному представлению.
- Команда уже присутствует в TUI во время выполнения и в `transcript`; доработка
  делает её видимой также после успешного завершения, но не создаёт новый
  источник данных и не изменяет формат rollout.
- Не следует заменять список семантическими подписями вроде `Search` или `Read`:
  для этого существуют рендерер групп `Exploring`/`Explored` и отдельная
  карточка core tool activity. Компактная группа успешных команд показывает
  фактический текст команды.
