---
id: fork-mcp-transport-recovery
status: active
created: 2026-07-30
updated: 2026-07-30
source_scope: hermione-0.146.0
---

# Восстановление транспорта MCP stdio

## Обзор

Карточка владеет узкой fork-доработкой, которая восстанавливает упавшее
соединение MCP stdio без сохранения диагностики, перехвата stderr и изменений
контекста, видимого модели.

| Поле | Значение |
| --- | --- |
| Статус карточки | `active` |
| Статус реализации | `implemented` |
| Пользовательская цель | После падения транспорта MCP автоматически восстановить соединение и не заставлять пользователя повторять вызов вручную |
| Область выполнения | Локальный MCP stdio и MCP stdio, запускаемый через executor |
| Правило восстановления | Повторно инициализировать транспорт и ровно один раз повторить исходную операцию |
| Поведение после смерти в простое | Пометить завершившийся запуск мёртвым и лениво восстановить его перед следующей операцией |
| Допущение повтора | Операции MCP считаются идемпотентными |
| Не входят в доработку | Диагностика rollout, хвост stderr, `launch_id`, привязка к thread, логи SQLite, UI и сообщения, видимые модели |
| Исторический источник | `docs/fork/mcp-rollout-diagnostics.md` |

Карточка `docs/fork/mcp-rollout-diagnostics.md` остаётся `reverted` и хранит
историю прежней объединённой реализации. Эта карточка становится единственным
активным владельцем поведения восстановления.

## Зачем это нужно

После смерти процесса MCP stdio сохранённый список инструментов может оставаться
доступным, хотя фактический транспорт уже закрыт. Следующий реальный вызов тогда
завершается `Transport closed` или ошибкой записи `BrokenPipe`.

Без автоматического восстановления пользователь получает ошибку и должен
вручную повторить действие либо перезапустить сессию. Требуемое поведение:

1. Обнаружить закрытый транспорт stdio или мёртвый дескриптор процесса.
2. Сериализовать восстановление, чтобы конкурентные вызовы не создавали несколько
   новых соединений.
3. Пересоздать транспорт из сохранённого `transport_recipe`.
4. Повторить инициализацию MCP с сохранённым `initialize_context`.
5. Заменить текущий `RunningService`.
6. Повторить исходную операцию ровно один раз.

Восстановление не должно создавать фоновый цикл перезапуска. Если процесс умер
в простое, новый запуск создаётся только при следующей реальной операции MCP.

## Карта файлов

| Файл | Роль |
| --- | --- |
| `codex-rs/rmcp-client/src/stdio_server_launcher.rs` | Хранит признак смерти процесса и отмечает запуск мёртвым после EOF транспорта или `BrokenPipe` |
| `codex-rs/rmcp-client/src/rmcp_client.rs` | Выполняет ленивое восстановление перед операцией, переподключение и однократный повтор |
| `codex-rs/rmcp-client/src/bin/test_stdio_server.rs` | Даёт управляемый тестовый сервер для одного или нескольких закрытий транспорта и завершения процесса в простое |
| `codex-rs/core/tests/suite/mcp_transport_recovery.rs` | Проверяет пользовательское поведение по полному пути вызова MCP в Codex |
| `codex-rs/core/tests/suite/mod.rs` | Подключает модуль интеграционных тестов |
| `docs/fork/mcp-transport-recovery.md` | Владеющий документ передачи доработки восстановления |
| `docs/fork/mcp-rollout-diagnostics.md` | Карточка прежней объединённой реализации со статусом `reverted`; содержит ссылку на нового владельца восстановления |

Намеренно не меняются:

| Зона | Причина |
| --- | --- |
| `codex-rs/protocol` | Восстановление не добавляет типы протокола или элементы rollout |
| `codex-rs/core/src/session` | Сохранение и реконструкция сессии не участвуют в восстановлении транспорта |
| `codex-rs/codex-mcp` | `RmcpClient` уже владеет рецептом транспорта, контекстом инициализации и повтором операции |
| `codex-rs/state`, `codex-rs/rollout`, `codex-rs/thread-store` | Никакие диагностические данные не сохраняются |
| Журналирование stderr MCP из upstream | Существующее поведение не расширяется и не является частью этой карточки |
| TUI и контекст модели | Восстановление прозрачно для модели и интерфейса, кроме итогового успеха либо ошибки операции |

## Итоговый контракт

### Область применения

Автоматическое восстановление после `TransportClosed` и `BrokenPipe`
применяется только к `TransportRecipe::Stdio`. Для Streamable HTTP сохраняется
существующее отдельное восстановление после `SessionExpired404`.

Транспорт in-process не получает новое правило повтора.

### Признак мёртвого запуска

Каждый `StdioServerProcessHandle` содержит только атомарный признак смерти:

- получение из транспорта возвращает EOF — запуск помечается мёртвым;
- отправка в транспорт возвращает `io::ErrorKind::BrokenPipe` — запуск помечается
  мёртвым;
- явное завершение через дескриптор процесса также помечает запуск мёртвым;
- признак не содержит `launch_id`, stderr, временную метку или диагностические
  метаданные.

Для локального и запускаемого через executor stdio используется один
`StdioServerTransport`, поэтому признак не требует отдельной подсистемы
журналирования процессов.

### Ленивое восстановление после смерти в простое

Перед каждой операцией MCP `RmcpClient` проверяет текущий дескриптор процесса
stdio. Если запуск мёртв:

1. Текущий `RunningService` фиксируется как сервис, на котором произошёл сбой.
2. Восстановление выполняется под `session_recovery_lock`.
3. Если другой конкурентный вызов уже заменил сервис, повторное подключение не
   выполняется.
4. Иначе создаётся новый транспорт, выполняется инициализация и заменяется
   сервис.
5. Операция выполняется на новом сервисе.

Фоновая задача для периодического перезапуска, задержки повторов или проверки
работоспособности не создаётся.

### Восстановление после ошибки операции

Если операция завершилась `ServiceError::TransportClosed` либо
`ServiceError::TransportSend` с `io::ErrorKind::BrokenPipe`, `RmcpClient`
запускает то же сериализованное восстановление и повторяет исходную операцию
ровно один раз.

Повтор вызывается напрямую, а не через рекурсивную точку входа восстановления.
Поэтому повторный `TransportClosed` или `BrokenPipe` возвращается вызывающему
коду и не создаёт второй цикл восстановления.

### Конкурентные вызовы

`session_recovery_lock` сериализует замену транспорта. После захвата блокировки
восстановление через `Arc::ptr_eq` сравнивает текущий сервис с сервисом, на
котором произошёл сбой:

- если сервис уже заменён, новый запуск не создаётся;
- если сервис прежний, только текущее восстановление создаёт транспорт и
  выполняет инициализацию;
- каждая конкурентная операция может повторить только собственный исходный
  вызов.

### Ошибки восстановления

Ошибка создания транспорта или инициализации возвращается наружу. Старый
закрытый транспорт не считается восстановленным, а автоматический повтор без
успешной повторной инициализации не выполняется.

### Журналирование и поведение, видимое модели

Доработка не добавляет:

- `RolloutItem::McpDiagnostic`;
- `McpDiagnosticItem` или `McpDiagnosticEvent`;
- ограниченный хвост stderr;
- диагностический контекст с `thread_id`, `turn_id`, `call_id` или `launch_id`;
- события tracing для восстановления;
- строки SQLite;
- вывод инструмента, элемент истории или фрагмент контекста модели.

Существующее журналирование stderr процесса MCP из upstream остаётся без
изменений, но не расширяется и не используется механизмом восстановления.

## Архитектурное решение

### Почему восстановление живёт в `RmcpClient`

`RmcpClient` уже хранит все необходимые зависимости:

- `transport_recipe` для повторного создания транспорта;
- `initialize_context` для повторного рукопожатия;
- `session_recovery_lock` для устранения дубликатов конкурентного
  восстановления;
- замыкание операции в `run_service_operation` для однократного повтора.

Перенос восстановления в `codex-mcp` или `core` потребовал бы протаскивать
состояние транспорта через дополнительные слои и дублировал бы уже существующее
восстановление HTTP-сессии.

### Почему признак смерти принадлежит дескриптору транспорта

Место запуска процесса различается для локального и запускаемого через executor
stdio, но оба варианта сходятся в `StdioServerTransport`. Атомарный признак даёт
`RmcpClient` минимальный сигнал жизнеспособности без API конкретного типа
процесса, буфера stderr или сохраняемого идентификатора.

Признак служит ранним сигналом, а ошибка транспорта остаётся обязательным
резервным механизмом: процесс может быть ещё формально жив, но уже не принимать
запросы.

### Отклонённые альтернативы

| Вариант | Почему не выбран |
| --- | --- |
| Восстановить всю `mcp-rollout-diagnostics.md` | Возвращает ненужные изменения протокола, сохранения состояния, stderr и контекста |
| Пересоздавать весь `McpServerConnection` в менеджере | Теряет локальность решения и дублирует состояние, которым уже владеет `RmcpClient` |
| Немедленный фоновый перезапуск после завершения процесса | Может создать цикл аварийных перезапусков без реального пользовательского вызова |
| Неограниченный повтор | Скрывает устойчивый отказ и может многократно выполнить побочные эффекты |
| Только проверка жизнеспособности процесса | Не обнаруживает закрытый транспорт при ещё живом процессе |

## Порядок повторения при переносе

1. Проверить текущую форму `RmcpClient`, `TransportRecipe`,
   `InitializeContext` и существующего восстановления HTTP-сессии.
2. Сохранить один заменяемый дескриптор процесса stdio на клиент.
3. Добавить минимальный признак смерти к дескриптору процесса и выставлять его
   при EOF, `BrokenPipe` и вызове `terminate`.
4. Перед операцией выполнять ленивое восстановление мёртвого запуска.
5. Направить `TransportClosed` и stdio `BrokenPipe` в существующую
   границу сериализованной повторной инициализации.
6. Повтор исходной операции выполнять один раз без рекурсивного входа в
   восстановление.
7. Перенести тестовый сервер и три интеграционных сценария, не возвращая
   проверки диагностических событий.
8. Проверить, что протокол, rollout, состояние, перехват stderr и данные,
   видимые модели, не изменились.

## Проверки

### Смысловое покрытие

| Контракт | Обязательность | Проверка |
| --- | --- | --- |
| Один `TransportClosed` пересоздаёт транспорт stdio и возвращает успешный результат | `required` | Интеграционный тест Core |
| Повторное закрытие после повтора возвращается наружу без второго цикла восстановления | `required` | Тестовый сервер закрывает первые два запуска и вернул бы успех на третьем |
| Завершение процесса в простое восстанавливается перед следующим реальным вызовом MCP | `required` | Два последовательных интеграционных хода Core |
| Восстановление поддерживает локальный и удалённый запуск через executor | `required` | `build_with_auto_env` и тестовый бинарник с поддержкой удалённого окружения |
| Восстановление не зависит от stderr или сохраняемой диагностики | `required` для проверки | Аудит изменённых исходников |
| Карточка не добавляет данных в протокол или контекст модели | `required` для проверки | Аудит diff |

### Владелец исполняемой карты

Проверки уровня карточки запускает команда `fork tests`, которой владеет skill,
для `fork-mcp-transport-recovery`. Внутренние `argv` хранятся ниже как данные
`fork-tests.v1`, а не как пользовательская инструкция по запуску.

```json
{
  "schema": "fork-tests.v1",
  "tests": [
    {
      "purpose": "бинарник тестового сервера восстановления MCP stdio",
      "argv": [
        "cargo",
        "build",
        "--manifest-path",
        "codex-rs/Cargo.toml",
        "-p",
        "codex-rmcp-client",
        "--bin",
        "test_stdio_server"
      ]
    },
    {
      "purpose": "переподключение после закрытия транспорта MCP и однократный повтор",
      "argv": [
        "just",
        "test",
        "-p",
        "codex-core",
        "mcp_transport_close_reinitializes_and_retries_once"
      ]
    },
    {
      "purpose": "повторное закрытие транспорта MCP останавливает восстановление",
      "argv": [
        "just",
        "test",
        "-p",
        "codex-core",
        "mcp_transport_second_close_is_not_retried_again"
      ]
    },
    {
      "purpose": "ленивое восстановление после завершения процесса MCP в простое",
      "argv": [
        "just",
        "test",
        "-p",
        "codex-core",
        "mcp_idle_process_exit_recovers_before_next_call"
      ]
    }
  ]
}
```

### Дополнительные gates

| Gate | Требование |
| --- | --- |
| Проверка формы fork-карточек | `required` после создания active-карточки |
| Печать исполняемой карты проверок уровня карточки | `required` после добавления `fork-tests.v1` |
| Форматирование Rust-кода | `required` после Rust-правок |
| Rust lint/fix для изменённых пакетов | `required` перед финализацией большой Rust-правки |
| Быстрая сборка fork | `required` после успешных проверок карточки |
| Генераторы схем | `not-applicable`: конфигурация, протокол и схема app-server не меняются |
| Полный набор тестов | Только по отдельному решению пользователя |
| Установка | Только по отдельному решению пользователя |

### Исторические результаты

| Проверка | Результат |
| --- | --- |
| Аудит исходников `rust-v0.146.0` | `passed`: существующие `transport_recipe`, `initialize_context` и `session_recovery_lock` можно переиспользовать |
| Сравнение с реализацией до отката | `passed`: путь восстановления отделим от диагностики rollout и перехвата stderr |
| Проверки текущей реализации на уровне карточки | `passed`: тестовый сервер и три интеграционных сценария |
| Форматирование и lint/fix | `passed`: изменённые пакеты проверены, форматирование применено |
| Быстрая сборка fork | `passed`: бинарник `release-fast` собран, метаданные и версия проверены |
| Локальная установка | `passed`: исходный, временный и установленный бинарники проверены штатным `fork install` |

### Известные падения и пропуски

- Фактический прогон с удалённым Docker executor в этой реализации не
  выполнялся; интеграционные тесты используют `build_with_auto_env` и
  путь тестового бинарника с поддержкой удалённого окружения для такого запуска.
- Полный набор тестов не запускался: он требует отдельного решения пользователя.

## Выполнение, сборка и установка

Доработка не добавляет ключей конфигурации, флагов возможностей или отдельных
команд времени выполнения. После сборки восстановление включено для всех
клиентов MCP stdio.

Новый бинарник не устанавливается автоматически. Установка принадлежит
отдельному шагу `fork install`, которым владеет skill, и требует решения
пользователя. В текущем проходе пользователь дал это решение, и локальная
установка прошла успешно.

## Риски и ограничения

- Операции MCP, включая `tools/call`, считаются идемпотентными. Если сервер
  выполнил побочный эффект, но транспорт закрылся до доставки ответа, повтор
  способен выполнить побочный эффект ещё раз.
- Признак смерти доказывает закрытие транспорта, но не является полной проверкой
  работоспособности. Зависший живой процесс без EOF и без ошибки отправки этой
  доработкой не восстанавливается.
- Ошибка запуска нового процесса или инициализации возвращается пользователю;
  повторная попытка самого восстановления отсутствует.
- Восстановление не перечитывает изменённую конфигурацию MCP. Оно повторно
  использует сохранённый `transport_recipe` текущего клиента.
- Фоновый перезапуск намеренно отсутствует, поэтому умерший в простое сервер
  остаётся остановленным до следующей операции.

## Проверка покрытия

| Смысловой пункт | Статус |
| --- | --- |
| Восстановление выделено из диагностической карточки со статусом `reverted` в отдельного активного владельца | перенесено в карточку |
| `TransportClosed` и `BrokenPipe` ведут к повторному подключению | перенесено в карточку и код |
| Исходная операция повторяется ровно один раз | перенесено в карточку и интеграционный тест |
| Повторный отказ транспорта не создаёт второй цикл восстановления | перенесено в карточку и интеграционный тест |
| Завершение процесса в простое восстанавливается лениво | перенесено в карточку и интеграционный тест |
| Конкурентное восстановление устраняет дубликаты по идентичности сервиса, на котором произошёл сбой | перенесено в карточку и код |
| Фоновый цикл аварийных перезапусков отсутствует | перенесено в карточку |
| Диагностика rollout и сохранение stderr не возвращаются | перенесено в карточку и границу diff |
| Риск повторного побочного эффекта принят явно | перенесено в карточку |
