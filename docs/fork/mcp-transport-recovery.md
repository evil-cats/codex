---
id: fork-mcp-transport-recovery
status: active
created: 2026-07-30
updated: 2026-09-09
---

# Восстановление транспорта MCP stdio

## Обзор

Карточка владеет узкой fork-доработкой, которая восстанавливает упавшее
соединение MCP stdio ограниченной серией повторных запусков без сохранения
диагностики, перехвата stderr и изменений контекста, видимого модели.

## Зачем это нужно

После смерти процесса MCP stdio сохранённый список инструментов может оставаться
доступным, хотя фактический транспорт уже закрыт. Следующий реальный вызов тогда
завершается `Transport closed` или ошибкой записи `BrokenPipe`.

Без автоматического восстановления пользователь получает ошибку и должен
вручную повторить действие либо перезапустить сессию. Требуемое поведение:

1. Обнаружить закрытый транспорт stdio или мёртвый дескриптор процесса.
2. Сериализовать восстановление, чтобы конкурентные вызовы не создавали несколько
   новых соединений.
3. Выполнить до пяти последовательных попыток пересоздать транспорт из
   сохранённого `transport_recipe` и повторить инициализацию MCP с сохранённым
   `initialize_context`.
4. Выдержать паузы `250`, `500`, `1_000` и `2_000` мс перед попытками со второй
   по пятую.
5. Закрыть неудачный запуск перед следующей попыткой и заменить текущий
   `RunningService` только успешно инициализированным сервисом.
6. Повторить исходную операцию ровно один раз после успешного восстановления.
7. После исчерпания серии вернуть последнюю ошибку; следующий внешний вызов
   начинает новую полную серию.

Восстановление не должно создавать фоновый цикл перезапуска. Если процесс умер
в простое, серия запусков начинается только при следующей реальной операции
MCP, а паузы существуют лишь внутри этой активной серии.

## Карта файлов

| Файл | Роль |
| --- | --- |
| `codex-rs/rmcp-client/src/stdio_server_launcher.rs` | Хранит признак смерти процесса и отмечает запуск мёртвым после EOF транспорта или `BrokenPipe` для вариантов `Local` и `Executor` |
| `codex-rs/rmcp-client/src/rmcp_client.rs` | Обнаруживает отказ перед операцией или по её результату, направляет его в recovery и повторяет исходную операцию не более одного раза |
| `codex-rs/rmcp-client/src/service_recovery.rs` | Сериализует замену сервиса, выполняет ограниченную серию stdio-запусков и классифицирует постоянные ошибки создания транспорта |
| `codex-rs/rmcp-client/src/service_recovery_tests.rs` | Проверяет, что fail-fast применяется только к `NotFound` и `PermissionDenied` создания транспорта, но не к ошибкам инициализации |
| `codex-rs/rmcp-client/src/streamable_http_retry.rs` | Ограничивает повторы инициализации HTTP-транспортом: stdio делегируется в `connect_pending_transport` без повторов |
| `codex-rs/rmcp-client/src/bin/test_stdio_server.rs` | Даёт управляемый тестовый сервер для конкурентных закрытий, `BrokenPipe`, ошибок запуска и инициализации; завершение процесса в простое использует `MCP_TEST_EXIT_FILE` |
| `codex-rs/rmcp-client/tests/mcp_2026_stdio.rs` | Подключает прямые интеграционные сценарии публичного `RmcpClient` |
| `codex-rs/rmcp-client/tests/mcp_2026_stdio/stdio_transport_recovery.rs` | Наблюдает число запусков, паузы, инициализации и вызовы операции при успешной серии, исчерпании попыток и конкурентном восстановлении, включая отправку нового запроса `events/stream` |
| `codex-rs/core/tests/suite/mcp_transport_recovery.rs` | Проверяет пользовательское поведение по полному пути вызова MCP в Codex |
| `codex-rs/core/tests/suite/mod.rs` | Подключает модуль интеграционных тестов |
| `docs/fork/mcp-transport-recovery.md` | Владеющий документ передачи доработки восстановления |

Намеренно не меняются:

| Зона | Причина |
| --- | --- |
| `codex-rs/protocol` | Восстановление не добавляет типы протокола или элементы rollout |
| `codex-rs/core/src/session` | Сохранение и реконструкция сессии не участвуют в восстановлении транспорта |
| `codex-rs/codex-mcp` | Startup grace, late startup и stable `McpBinding` остаются в connection manager; recovery меняет сервис внутри уже привязанного `RmcpClient` |
| `codex-rs/state`, `codex-rs/rollout`, `codex-rs/thread-store` | Никакие диагностические данные не сохраняются |
| Журналирование stderr MCP из upstream | Существующее поведение не расширяется и не является частью этой карточки |
| TUI и контекст модели | Восстановление прозрачно для модели и интерфейса, кроме итогового успеха либо ошибки операции |

## Итоговый контракт

### Область применения

Автоматическое восстановление после `TransportClosed` и `BrokenPipe`
применяется только к `TransportRecipe::Stdio`. Это одинаково относится к
legacy lifecycle и к явно включённому MCP `2026-07-28`: повторный запуск
использует сохранённый `McpProtocolMode`, а
`connect_pending_transport_with_initialize_retries` для stdio без повторов
делегирует в `connect_pending_transport`, который повторно выбирает
соответствующий `ClientLifecycleMode`. Повторы инициализации, предназначенные
только для HTTP, не распространяются на stdio. Для Streamable HTTP сохраняется
существующее отдельное восстановление после `SessionExpired404`.
Варианты `PendingTransport::StreamableHttpWithOAuth` и
`PendingTransport::StreamableHttpWithAccessTokenOnly` не получают stdio-дескриптор
и не входят в эту ветку восстановления.

Транспорт in-process не получает новое правило повтора.

### Признак мёртвого запуска

Каждый `StdioServerProcessHandle` содержит только атомарный признак смерти:

- получение из транспорта возвращает EOF — запуск помечается мёртвым;
- отправка в транспорт возвращает `io::ErrorKind::BrokenPipe` — запуск помечается
  мёртвым;
- явное завершение через дескриптор процесса также помечает запуск мёртвым;
- тестовый `MCP_TEST_EXIT_FILE` потребляется перед завершением процесса, поэтому
  восстановленный процесс не наследует уже сработавший маркер выхода;
- признак не содержит `launch_id`, stderr, временную метку или диагностические
  метаданные.

Признак `dead` независим от `terminated` и конкретного локального механизма
завершения дерева процессов, включая Windows-варианты `Job` и `Process`:
усиленная очистка процессов не заменяет сигнал для ленивого восстановления
транспорта.

Для единого локального варианта `Local(LocalStdioTransport)`, сохраняющего
выбранный `McpProtocolMode`, и варианта `Executor(ExecutorProcessTransport)`
используется один `StdioServerTransport`, поэтому признак не требует отдельной
подсистемы журналирования процессов. Различие вариантов сохраняется только во
внутренней диспетчеризации `send`/`receive`/`close`.

### Ленивое восстановление после смерти в простое

Перед каждой операцией MCP `RmcpClient` проверяет текущий дескриптор процесса
stdio. Если запуск мёртв:

1. Текущий `RunningService` фиксируется как сервис, на котором произошёл сбой.
2. Восстановление выполняется под `session_recovery_lock`.
3. Если другой конкурентный вызов уже заменил сервис, повторное подключение не
   выполняется.
4. Иначе под той же блокировкой начинается ограниченная серия восстановления.
5. Первая попытка запускается немедленно; перед следующими четырьмя выдерживаются
   паузы `250`, `500`, `1_000` и `2_000` мс.
6. Каждая попытка создаёт новый транспорт и выполняет инициализацию. Неудачный
   запуск закрывается, а `RunningService` заменяется только после успешной
   инициализации.
7. Операция выполняется на новом сервисе.

Все публичные операции, создающие запрос MCP через текущий `RmcpClient`, входят
в эту границу. Текущий метод `send_event_stream_request` также
вызывает `run_service_operation`, поэтому отправка `events/stream` не обходит
проверку мёртвого stdio-процесса и однократный повтор ошибки отправки.

Фоновая задача для периодического перезапуска или проверки работоспособности не
создаётся. Задержки выполняются только внутри активной сериализованной серии
восстановления.

### Граница серии повторных запусков

Одна серия содержит не более пяти попыток создания транспорта и инициализации:
первая выполняется сразу, остальные — после фиксированных пауз `250`, `500`,
`1_000` и `2_000` мс. Суммарная задержка между попытками равна `3_750` мс без
учёта времени самого запуска и инициализации.

Расписание не получает jitter и отдельные ключи конфигурации. Единственный
владелец серии — восстановление уже успешно инициализированного stdio-клиента;
правила первоначального startup остаются отдельным контрактом.

### Граница startup и stable bindings

Recovery применяется только к уже успешно инициализированному `RmcpClient`.
Необязательный сервер, который не успел за общий startup grace и был исключён
из текущего stable `McpBinding`, не запускается и не восстанавливается этой
доработкой. Late startup и повторное формирование каталога остаются контрактом
connection manager.

Согласование состояния `pending` при startup и замена `trusted_access` после
первого `Failed` принадлежат `fork-mcp-startup-refresh-serialization` и не входят
в восстановление транспорта.

Если готовый сервер умер после захвата binding, подготовленный вызов продолжает
использовать тот же `Arc<RmcpClient>`. Recovery заменяет только внутренний
`RunningService`, не подменяет захваченный client и не меняет revision каталога.
Поэтому однократный повтор остаётся частью исходной operation и не превращается
в маршрутизацию вызова на поздно появившийся либо другой client.

### Восстановление после ошибки операции

Если операция завершилась `ServiceError::TransportClosed` либо
`ServiceError::TransportSend` с `io::ErrorKind::BrokenPipe`, `RmcpClient`
запускает ту же сериализованную серию восстановления и повторяет исходную
операцию ровно один раз после успешной инициализации нового сервиса.

Повтор вызывается напрямую, а не через рекурсивную точку входа восстановления.
Поэтому повторный `TransportClosed` или `BrokenPipe` возвращается вызывающему
коду и не создаёт вторую серию восстановления в рамках той же исходной
операции.

### Конкурентные вызовы

`session_recovery_lock` сериализует всю серию, включая паузы, создание транспорта,
инициализацию и замену сервиса. После захвата блокировки восстановление через
`Arc::ptr_eq` сравнивает текущий сервис с сервисом, на котором произошёл сбой:

- если сервис уже заменён, новый запуск не создаётся;
- если сервис прежний, только текущее восстановление выполняет до пяти
  последовательных запусков;
- каждая конкурентная операция может повторить только собственный исходный
  вызов.

После успешной серии ожидающие блокировку вызовы используют новый сервис и не
запускают собственную серию. Если серия исчерпана и старый сервис остался
закрытым, следующий внешний вызов, включая уже ожидающий блокировку конкурентный
вызов, может начать новую полную серию; параллельные запуски по-прежнему
невозможны.

### Ошибки восстановления

Ошибка создания транспорта или инициализации завершает текущую попытку. Перед
паузой и следующим запуском созданный этой попыткой транспорт закрывается, чтобы
серия не оставляла дочерние процессы и другие ресурсы.

Явно постоянные ошибки создания транспорта, включая
`io::ErrorKind::NotFound` и `io::ErrorKind::PermissionDenied`, прекращают серию
сразу. Остальные ошибки создания транспорта и инициализации повторяются в
границах пяти попыток. После исчерпания серии наружу возвращается последняя
ошибка, старый закрытый сервис не считается восстановленным, а исходная операция
не выполняется. Следующий внешний вызов начинает новую серию с первой попытки.

### Журналирование и поведение, видимое модели

Доработка не добавляет:

- `RolloutItem::McpDiagnostic`;
- `McpDiagnosticItem` или `McpDiagnosticEvent`;
- ограниченный хвост stderr;
- диагностический контекст с `thread_id`, `turn_id`, `call_id` или `launch_id`;
- события tracing для восстановления;
- строки SQLite;
- вывод инструмента, элемент истории или фрагмент контекста модели.

Существующее журналирование stderr процесса MCP из upstream остаётся без
изменений, но не расширяется и не используется механизмом восстановления.

## Архитектурное решение

### Почему восстановление живёт в `RmcpClient`

`RmcpClient` уже хранит все необходимые зависимости:

- `transport_recipe` для повторного создания транспорта;
- `initialize_context` для повторного рукопожатия;
- `session_recovery_lock` для устранения дубликатов конкурентного
  восстановления;
- фиксированное ограниченное расписание повторных запусков stdio;
- замыкание операции в `run_service_operation` для однократного повтора.

Перенос восстановления в `codex-mcp` или `core` потребовал бы протаскивать
состояние транспорта через дополнительные слои и дублировал бы уже существующее
восстановление HTTP-сессии.

### Почему серия ограничена и не настраивается

После обновления внешнего сервера stdio-процесс MCP может завершиться раньше,
чем сервер снова станет доступен. Единственный немедленный запуск превращает
этот короткий переход в ошибку пользователя, а фоновый цикл создаёт отдельный
lifecycle и продолжает работу без внешнего запроса.

Пять попыток с суммарной паузой `3_750` мс дают серверу ограниченное окно для
возврата и сохраняют конечное время ответа. Jitter не нужен, потому что
`session_recovery_lock` допускает только одну активную серию. Отдельные ключи
конфигурации не добавляются: расписание является внутренним контрактом recovery,
а не пользовательской политикой первоначального startup.

### Почему признак смерти принадлежит дескриптору транспорта

Место запуска процесса различается для локального и запускаемого через executor
stdio, но оба варианта сходятся в `StdioServerTransport`. Атомарный признак даёт
`RmcpClient` минимальный сигнал жизнеспособности без API конкретного типа
процесса, буфера stderr или сохраняемого идентификатора.

Признак служит ранним сигналом, а ошибка транспорта остаётся обязательным
резервным механизмом: процесс может быть ещё формально жив, но уже не принимать
запросы.

## Порядок повторения при переносе

1. Проверить текущую форму `RmcpClient`, `TransportRecipe`,
   `InitializeContext` и существующего восстановления HTTP-сессии.
2. Сохранить один заменяемый дескриптор процесса stdio на клиент, не меняя
   stable binding connection manager; вспомогательная функция извлечения
   дескриптора должна исчерпывающе возвращать `None` для всех вариантов
   `PendingTransport`, кроме `Stdio`.
3. Добавить минимальный признак смерти к дескриптору процесса и выставлять его
   при EOF, `BrokenPipe` и вызове `terminate`; оборачивать этим признаком варианты
   `Local` и `Executor`, сохраняя выбранный `McpProtocolMode` локального запуска.
4. Перед операцией выполнять ленивое восстановление мёртвого запуска.
5. Направить `TransportClosed` и stdio `BrokenPipe` в одну сериализованную
   серию из пяти попыток с паузами `250`, `500`, `1_000` и `2_000` мс, повторно
   используя сохранённые protocol mode и lifecycle. Не смешивать эту серию с
   повторами инициализации, предназначенными только для HTTP.
6. После каждой неудачной попытки закрывать созданный транспорт; постоянные
   `NotFound` и `PermissionDenied` возвращать сразу, а старый `RunningService`
   заменять только успешно инициализированным сервисом.
7. Повтор исходной операции выполнять один раз без рекурсивного входа в
   восстановление; после исчерпания серии разрешать следующему внешнему вызову
   начать новую серию.
8. При появлении новых публичных операций проверить, что они используют
   `run_service_operation`, а не получают `RunningService` напрямую.
9. Сохранить управляемый тестовый сервер и интеграционные сценарии, используя
   `MCP_TEST_EXIT_FILE` для завершения процесса в простое и не возвращая
   отдельный планировщик выхода для восстановления или проверки диагностических
   событий.
10. Проверить, что протокол, rollout, состояние, перехват stderr и данные,
   видимые модели, не изменились.

## Проверки

Исполняемая карта card-level regression tests:

```json
{
  "schema": "fork-tests.v1",
  "tests": [
    {
      "purpose": "бинарник тестового сервера восстановления MCP stdio",
      "argv": [
        "cargo",
        "build",
        "--manifest-path",
        "codex-rs/Cargo.toml",
        "-p",
        "codex-rmcp-client",
        "--bin",
        "test_stdio_server"
      ]
    },
    {
      "purpose": "fail-fast классификация постоянных ошибок создания транспорта без подавления повторяемых ошибок инициализации",
      "argv": [
        "just",
        "test",
        "-p",
        "codex-rmcp-client",
        "stdio_recovery_error_classification"
      ]
    },
    {
      "purpose": "ограниченная серия повторных запусков, конкурентное восстановление, BrokenPipe, events/stream после смерти в простое и ошибки восстановления для stdio lifecycle MCP 2026-07-28",
      "argv": [
        "just",
        "test",
        "-p",
        "codex-rmcp-client",
        "--test",
        "mcp_2026_stdio"
      ]
    },
    {
      "purpose": "серия переподключений после закрытия транспорта MCP и не более одного повтора исходной операции",
      "argv": [
        "just",
        "test",
        "-p",
        "codex-core",
        "mcp_transport_close_reinitializes_and_retries_once"
      ]
    },
    {
      "purpose": "повторное закрытие транспорта после восстановления не запускает вторую серию в рамках исходной операции",
      "argv": [
        "just",
        "test",
        "-p",
        "codex-core",
        "mcp_transport_second_close_is_not_retried_again"
      ]
    },
    {
      "purpose": "ленивая серия восстановления после завершения процесса MCP в простое",
      "argv": [
        "just",
        "test",
        "-p",
        "codex-core",
        "mcp_idle_process_exit_recovers_before_next_call"
      ]
    }
  ]
}
```

Target `mcp_2026_stdio` проверяет через публичный `RmcpClient`:

- два конкурентных сбоя одного `RunningService`, одна успешная серия запусков и
  независимый однократный повтор обеих операций;
- настоящий `TransportSend(BrokenPipe)` при закрытом stdin и удерживаемом stdout;
- ленивое восстановление перед отправкой нового `events/stream` после смерти
  stdio-процесса в простое;
- успех на второй и пятой попытках с паузами `250`, `500`, `1_000` и `2_000` мс
  без вызова исходной операции до успешной инициализации;
- закрытие транспорта и процесса каждой неудачной попытки;
- немедленный возврат `NotFound` и `PermissionDenied` без оставшихся запусков;
- исчерпание пяти попыток с возвратом последней ошибки без замены старого
  закрытого сервиса и без вызова исходной операции;
- новую полную серию при следующем внешнем вызове после исчерпания предыдущей;
- отсутствие параллельных запусков при конкурентном восстановлении.

Unit test `stdio_recovery_error_classification_distinguishes_creation_from_initialize_failures`
отдельно проверяет границу немедленного завершения серии для ошибок создания
транспорта.

## Риски и ограничения

- Операции MCP, включая `tools/call`, считаются идемпотентными. Если сервер
  выполнил побочный эффект, но транспорт закрылся до доставки ответа, повтор
  способен выполнить побочный эффект ещё раз.
- Признак смерти доказывает закрытие транспорта, но не является полной проверкой
  работоспособности. Зависший живой процесс без EOF и без ошибки отправки этой
  доработкой не восстанавливается.
- Повторяемая ошибка запуска или инициализации возвращается пользователю после
  пятой попытки. К фиксированным паузам до `3_750` мс добавляется длительность
  каждого запуска и рукопожатия.
- После исчерпания серии следующий внешний вызов начинает новую серию. Несколько
  конкурентных вызовов при устойчивом отказе могут последовательно выполнить
  несколько полных серий, хотя параллельные запуски исключены блокировкой.
- `NotFound` и `PermissionDenied` считаются постоянными только на этапе создания
  транспорта и завершают серию сразу; остальные ошибки инициализации остаются
  повторяемыми и могут задержать возврат детерминированной ошибки.
- Восстановление не перечитывает изменённую конфигурацию MCP. Оно повторно
  использует сохранённый `transport_recipe` и выбранный protocol mode текущего
  клиента.
- Фоновый перезапуск намеренно отсутствует, поэтому умерший в простое сервер
  остаётся остановленным до следующей операции.
- Сервер, не вошедший в stable binding из-за startup grace или late startup, не
  получает recovery-вызов до появления в новом binding; эта карточка не меняет
  правила публикации каталога.
